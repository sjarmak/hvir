interface:
  display_name: 'hvir Merge PR'
  short_description: 'Merge one approved hvir PR'
  default_prompt: 'Use $hvir-merge-pr to merge one explicitly approved hvir pull request through GitHub and reconcile its Project fields.'
policy:
  allow_implicit_invocation: false
