{"_type":"issue","id":"hvir-14d","title":"ADR-055: read-back navigation reaches a mirrored program without being typing","description":"hvir-9as sends PageUp/PageDown from a phone read-back gesture to a program that owns its own history, on a mirror whose per-session arm has lapsed, and keeps those bytes out of the desktop's terminal-input record. Both halves narrow written decisions, so they need a record before the code lands.\n\nWhat it narrows:\n- ADR-050:65-69 states 'Typing from the phone is an explicit act three times over. Settings must allow it, and the default is off. A mirror opens with input disabled and the user arms it per mirror.' Read-back navigation keeps the first two and drops the third. Record why the owner's permission is the boundary that matters and the per-mirror arm is not: the arm exists so a phone left unlocked in a pocket cannot type into a session, and a gesture a person is actively making on a screen they are looking at is not that risk. State the closed set of bytes this admits, which is exactly the two the shared wheel policy emits for an alternate-screen gesture, and that nothing else is ever exempt.\n- ADR-050:74-76 states that main tells the owning renderer that mirror input reached its PTY and the renderer records it as terminal input, which arms ADR-019 and produces the next Ready and Push. Read-back navigation must not be recorded that way, or paging history on a phone produces a push notification about the person's own scrolling. Record the distinction and note that companion-sessions.ts:201-203 already draws the same one for resize.\n- ADR-053's decision 1 says an alternate-screen session states it has no history to read back. That is false for any program holding its own history. Amend it: the emulator has no scrollback, the program may still have history, and the phone pages it the way the desktop does.\n\nConsequences to state plainly:\n- Paging a program's own history is not private to the phone. It is the same program, so the desktop's view moves too. Emulator-viewport read-back does not do this, so the two mechanisms behave differently in a way a person will notice.\n- A program is free to bind PageUp and PageDown to something other than scrolling. The decision admits that: it is the same bet the desktop's wheel policy already makes, and parity with the desktop is the requirement.\n\nEnd with a revisit clause naming what would justify widening the exempt set.","acceptance_criteria":"docs/adr/ADR-055-*.md exists in the repo's ADR shape; npm run check-adrs passes with reciprocal relationship lines against ADR-050 and ADR-053. The record names the exact exempt byte set, states the attention distinction, and carries the shared-view consequence. No em dashes.","notes":"Filed 2026-09-19 after the maintainer saw the false notice on a real phone. hvir-9as depends on this landing first, the way hvir-coq depended on ADR-054.","status":"closed","priority":0,"issue_type":"decision","assignee":"sjarmak","created_at":"2026-09-19T22:13:26Z","created_by":"sjarmak","updated_at":"2026-09-19T22:16:36Z","started_at":"2026-09-19T22:16:33Z","closed_at":"2026-09-19T22:16:36Z","close_reason":"ADR-055 landed in d1fda06; check-adrs passes with reciprocal lines against ADR-050 and ADR-053.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-9as","title":"The phone cannot read back a session whose program owns its history, and says there is none","description":"Observed 2026-09-19 on a real phone against the 'Mayor Cairn' session: Claude Code running under tmux. The phone shows 'This program draws its whole screen, so there is no history to read back.' and a drag moves nothing. The desktop reads back the same session fine.\n\nThe notice is false. tmux holds a full history; it just holds it itself rather than in the emulator's scrollback. isAlternateScreen() is correct, but the sentence the page derives from it is not: what is true is that the EMULATOR has no scrollback, not that the SESSION has no history. ADR-053's decision 1 ('an alternate-screen session states that it has no history to read back', written 2026-09-19) is wrong as stated, and wrong in exactly the case the Companion exists for. A shell under tmux, a pager, and anything else holding its own history all land here.\n\nBoth read-back mechanisms already exist and route correctly. src/shared/terminal-wheel.ts:83-87 sends PageUp/PageDown to an alternate-screen program, which is how the desktop scrolls this session: tmux redraws with older content and the phone mirrors the redraw. That is why the phone moves only when the desktop scrolls. What is missing is that the phone cannot produce those keys for itself.\n\nThree parts, and none of them is the emulator-viewport read-back, which is right and stays.\n\n1. The gate. Page keys from a read-back gesture are input bytes, so ghostty-companion-pane's emitUser drops them on a disarmed mirror. Decided by the maintainer 2026-09-19: send them when the owner's mirrorInputAllowed is on, even after the per-mirror arm has lapsed, and never otherwise. Only the two navigation keys a read-back gesture produces, never typing. This is a page-side change: main's gate at companion-sessions.ts:195 is typingAllowed(), the owner's setting alone, so main already permits exactly this.\n\n2. The attention leak, found while tracing part 1 and the reason this is P0 rather than P2. pty-supervisor.ts:402 fans every mirror write out to mirrorInputListeners, and ADR-050:74-76 says the renderer records that as terminal input, which arms ADR-019 and produces the next Ready and the next Push. Without a distinction, paging back on the phone generates a push notification because you scrolled. Read-back navigation must reach the PTY without being recorded as terminal input, the same distinction companion-sessions.ts:201-203 already draws for resize ('Resizing is not input, so the typing permission does not gate it'). This crosses the wire and reaches the supervisor's fan-out.\n\n3. The notice. Stop claiming the session has no history. State what is true and what to do: the program keeps its own history and a drag pages it. Say nothing about arming if part 1 lands, since it will not be needed.\n\nKnown asymmetry to state somewhere a person will read it: emulator-viewport read-back is private to the phone, but paging tmux moves the desktop's view too, because it is the same program. That is inherent to history the program owns.","design":"Do not solve this by giving the page its own scrollback again. The emulator-viewport read-back is correct for normal-screen sessions and stays; this adds the second mechanism for history the program owns, which the gesture policy already decides between.","acceptance_criteria":"On a phone mirroring Claude Code under tmux with the owner's input permission on and the per-mirror arm lapsed, a drag pages back through tmux history and the notice does not claim there is none. With the owner's permission off, nothing is sent and the page says why. Paging back produces no terminal-input record, no Ready and no Push on the desktop. Typing still requires the per-mirror arm. Verified on a real device, not only against a fake.","notes":"Supersedes part of ADR-053's decision 1 and narrows ADR-050:65-69 ('Typing from the phone is an explicit act three times over'), so it needs a record; see the sibling ADR bead. Found because the maintainer looked at a real phone, which is exactly what hvir-w00.7 exists to do.","status":"closed","priority":0,"issue_type":"bug","assignee":"sjarmak","created_at":"2026-09-19T22:13:09Z","created_by":"sjarmak","updated_at":"2026-09-19T22:31:36Z","started_at":"2026-09-19T22:31:36Z","closed_at":"2026-09-19T22:31:36Z","close_reason":"Landed in a4c2bb2. Page keys leave the pane on onNavigation past the arm, the wire admits navigation:true only for those two keys, main takes lease.navigate (no input fan-out, so no ADR-019 arming and no Push), and the notice states the program keeps its own history. Device verification on a real phone under tmux is still outstanding; tracked on hvir-w00.7.","dependencies":[{"issue_id":"hvir-9as","depends_on_id":"hvir-14d","type":"blocks","created_at":"2026-09-19T18:13:33Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-dzz","title":"A read-back drag pages at the speed a finger asks for, and loses nothing to event batching","description":"Found on a real phone 2026-09-19, paging tmux through the ADR-055 path: the read-back is jumpy and travels far more than the finger asks.\n\nTwo defects in src/shared/terminal-wheel.ts, which the desktop pane and the Companion mirror share.\n\n1. Step distance ignores what a step costs. normalizedWheelDelta divides every route by cellHeight * LINES_PER_WHEEL_STEP, so one step is three cells of travel whether that step scrolls three rows (viewport route) or a whole screen (page route). On a 132x43 grid scaled onto a phone that is roughly 20 on-screen pixels per PageUp, so one swipe covers about thirty pages. The gain is off by more than an order of magnitude, and being quantized to whole pages is what makes it read as jumpy rather than fast.\n\n2. The per-event clamp discards its overflow. consumeSteps banks only the sub-step fraction and then clamps whole steps to the route limit (1 on the page route), so the steps it clamped away are lost. How far a gesture travels then depends on how the browser batched touchmove rather than on the distance covered, which is the property the remainder banking directly above it exists to provide.\n\nDecided with the maintainer 2026-09-19: fix both. A drag pays for a page with half the rows it moves, so dragging half a screen moves a screen; a notch keeps its three lines; an SGR report stands for a notch rather than a screen and keeps them too. The clamped overflow is banked rather than dropped, bounded by one event's worth so a synthetic or accelerated delta is rate limited instead of queued.","design":"The kind of gesture travels on the event as a required field rather than being sniffed from deltaMode: Chrome reports pixel deltas for plain mouse wheels on macOS, so deltaMode does not separate a notch from a finger. The touch adapter is the one place that knows a finger produced the event. This keeps ADR-053's one policy (same routes, same bytes, one decision about what a gesture means) and varies only what one step of travel costs.\n\nBuilding the event from a DOM WheelEvent must read its fields explicitly: deltaY and the rest are prototype accessors, so a spread yields an empty object.","acceptance_criteria":"A drag on the alternate screen sends one page key per half screen of travel and a notch still sends one per three lines; an SGR report keeps three lines on either. Steps a clamped event could not carry are delivered by the following events rather than lost, so equal distance travels equally however the browser batched it, and a single absurd delta queues no more than one event's worth. Verified on a real device against tmux.","status":"closed","priority":1,"issue_type":"bug","assignee":"sjarmak","created_at":"2026-09-19T23:47:27Z","created_by":"sjarmak","updated_at":"2026-09-19T23:53:23Z","started_at":"2026-09-19T23:47:33Z","closed_at":"2026-09-19T23:53:23Z","close_reason":"Landed. stepCells charges a drag half the rows it moves for a page key, a notch keeps three lines, SGR keeps three lines on either; consumeSteps banks the clamped overflow bounded by one event's worth. The gesture kind travels on the event. Feel still wants confirming in the hand; noted on hvir-w00.7.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-w00.7","title":"Verify the phone read-back on a real device against a real session","description":"Everything in 07fd726, 3d572be and 5d1cbed is verified against faked emulators. Four behaviours have never run against the real ghostty build under a real finger, and each is the kind that passes a mocked test while being wrong in the hand:\n\n1. Scroll direction. The adapter computes scrollLines(deltaY / cellHeight) with deltaY = lastY - currentY, verified against ghostty-web.js:4474-4481 and :5056-5062 by reading the built source. If the fake encodes the axis the same way the code does, both are wrong together and every direction test passes. A phone that scrolls backwards is the single most likely defect in this epic.\n\n2. Feel. Touch deltas are divided by the mount's surface scale and sub-cell distance is banked rather than rounded. Whether that tracks the finger at a 132-column desktop grid scaled onto a phone is a judgement no test makes.\n\n3. The soft keyboard. ghostty-web focuses its hidden textarea on every touchend regardless of disableStdin (ghostty-web.js:4695-4698 into :4619-4624), and the page stops that in the capture phase. If the interception misses, each flick raises the keyboard, which resizes the viewport, which asks the desktop for a new PTY grid while Away.\n\n4. The return-to-live control: whether it appears when expected, whether one tap reaches the newest output over a grid taller than the phone, and whether it is reachable and legible at the top of the area.\n\nDo this against a real Claude Code session (which draws on the alternate screen, so it must show the no-history notice and no control) and a real plain shell with genuine scrollback. Extend the companion-mirror-away smoke scenario where a behaviour can be pinned there; it is the only altitude in this repo where the real emulator runs.\n\nIf the feel is wrong, say so and stop rather than tuning constants: the design was chosen from an argument, not from a phone, and the maintainer reserved the right to reject it in the hand.","acceptance_criteria":"A plain shell mirror scrolls back by finger in the right direction at a comfortable rate, in the session's colours. A full-screen session shows the live screen, the line 'This program keeps its own history. Drag to page back through it.', and no return-to-live control; a drag pages that program's own history (ADR-055) with the owner's permission on and the per-mirror arm lapsed, and produces no Ready and no Push on the desktop. A flick does not raise the soft keyboard. Over a grid taller than the phone, one tap on the control reaches the newest output. The companion-mirror-away smoke scenario covers whatever of this it can.","notes":"Partially executed 2026-09-19 by the maintainer looking at a real phone. It immediately found what no faked-emulator test could: for a Claude Code session under tmux, the phone showed 'no history to read back' and a drag moved nothing, while the desktop read the same session back fine. Filed as hvir-9as (P0) and hvir-14d (ADR-055); both landed 2026-09-19 (d1fda06, a4c2bb2).\n\nSecond device pass the same day found the paging gain wrong: about twenty on-screen pixels per PageUp at a scaled desktop grid, roughly thirty pages to a swipe, and the per-event clamp discarding its overflow so the same drag landed differently depending on touchmove batching. Filed and fixed as hvir-dzz (dc70a24): a drag now pays half the rows it moves for a page, a notch keeps three lines, and the clamped steps are banked. That gain was chosen from an argument again, not from a phone, so it is this bead's job to confirm half-a-screen-per-page is right in the hand.\n\nThe same pass confirmed the half-black terminal area is ADR-052 working as designed: a 132x43 grid fitted to a portrait phone by width can only be short, and the phone asks for its own grid only while the desktop is unfocused. Not a defect; possibly still a product question about a person holding their phone at their desk.\n\nStill unverified and still this bead's job:\n- scroll direction under a real finger on a NORMAL-screen session with genuine emulator scrollback. The tmux case exercises the page-key path, not the viewport path, so the direction question is untouched by either finding.\n- the feel of the delta-to-rows conversion at a scaled desktop grid, and of the new half-screen-per-page gain.\n- whether the capture-phase touchend interception actually stops the soft keyboard.\n- the return-to-live control's appearance, reachability and one-tap behaviour over a grid taller than the phone.\n- that a drag actually pages tmux from a mirror nobody armed, that the desktop gets no Ready and no Push from it, and that the desktop's own view moving with the phone is tolerable in the hand.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-19T17:59:02Z","created_by":"sjarmak","updated_at":"2026-09-19T23:53:41Z","dependencies":[{"issue_id":"hvir-w00.7","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T13:59:02Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-w00.6","title":"ADR-054: main carries sticky terminal modes ahead of the replayed tail","description":"hvir-coq makes main scan the PTY stream for a closed set of sticky DEC private modes and emit their state ahead of the replayed bytes. That crosses a boundary this codebase states in writing: ADR-050:50 reads 'Bytes cross from main to the phone as they are. Main does not interpret them', its rejected alternatives refuse main holding terminal state (ADR-050:123-125), and src/main/companion/companion-page-mirror.ts:9-10 restates it at the code level ('Bytes pass through untouched in both directions; nothing here logs, trims, or composes them').\n\nShipping the scanner without a record leaves the codebase contradicting itself, so this lands in the same wave as hvir-coq, not after it.\n\nThe record must fix the boundary precisely: main holds the on/off state of a named, closed set of modes and nothing else. No screen model, no cursor, no cell grid, no content. Name what is carried (1049 and 47, the alternate-screen family) and name what is deliberately not carried and why: synchronized output ?2026 (a dangling set would freeze the phone's first frame, so it is suppressed rather than replayed), ?1048 (save/restore cursor, an action with no steady state, which would jump the cursor mid-replay), ?1047 (unless the scanner also models that 1047l clears the screen), the mouse tracking family (carrying it makes hasMouseTracking() true and terminal-wheel.ts:60-79 then consumes every gesture as SGR reports that a disarmed mirror drops, silently disabling the phone's read-back), bracketed paste, application cursor keys, charset designation, keypad mode, DECSTBM and SGR.\n\nAlso record that the preamble is a distinct wire field, not a prefix of tail: a prefix is unshippable because sessions-companion.ts:248-249 rejects an opened event whose tail exceeds 256K and the mirror tail saturates at exactly that, and because companion-mirror-feed.ts:51,:100 re-slices the retained text from the front on every remount.\n\nConsequences: ADR-050:101-102 ('A raw tail replays an imperfect first frame until the program redraws') is narrowed for the carried modes only; the rejected alternative at ADR-050:123-125 (a headless emulator in main) stays rejected. End with a revisit clause naming the modes left out.","acceptance_criteria":"docs/adr/ADR-054-*.md exists in the repo's ADR shape and npm run check-adrs passes with reciprocal relationship lines against ADR-050. The Decision names the carried set, states the ?2026 suppression, and states the preamble is a distinct field. No em dashes.","notes":"Split out of hvir-w00.2 during the 2026-09-19 grounding pass. AGENTS.md's one-decision-per-record rule forbids folding it into ADR-053. docs/design.md is a single index file, so both records and every lifecycle edit are owned by one bead in the wave to keep file sets disjoint; this bead and hvir-w00.2 must therefore be implemented by the same hand or strictly in sequence.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-19T15:04:32Z","created_by":"sjarmak","updated_at":"2026-09-19T16:05:45Z","closed_at":"2026-09-19T16:05:45Z","close_reason":"Wave 1 implemented and verified: check-adrs, check-seams, lint, typecheck and the full suite (469 files, 4081 tests) pass. architecture:check is environment-blocked in this clone (scripts/architecture-github.mts requires origin jarmak-personal/hvir; this tree is sjarmak/hvir); architecture:report is clean with a budget-violation set identical to the pre-change tree.","dependencies":[{"issue_id":"hvir-w00.6","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T11:04:32Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-w00.3","title":"Read back through the emulator's viewport and delete the page's scrollback layer","description":"Make the emulator's own viewport the phone's read-back, per ADR-053.\n\nDelete:\n- src/renderer/companion/src/companion-mirror-history.ts entire (MirrorHistory, historyText, HISTORY_LINE_LIMIT, HISTORY_REFRESH_MS) and test/companion-mirror-history.test.ts.\n- The history box and its scrollback slice in companion-terminal-mount.ts: the \u003cpre\u003e, its font/lineHeight sizing, the scrollback() slice at :213-217, and the history's share of fit() at :230-240.\n- bufferLines and CompanionBufferLine from companion-terminal-pane.ts, their implementation in ghostty-companion-pane.ts:96-105, the stub in test/companion-terminal-pane-seam.test.ts:38, and the buffer rows in test/companion-page-fixture.ts:216,258. The mount's history is the only caller; the seam conformance test pins only mount/write/resize/dispose/onData, so removing the member does not break it.\n- .companion-terminal-history in styles.css:439-443, whose hardcoded #e8eaee is the epic's second observed defect.\n\nThis also retires the missing-spaces bug without fixing it. ghostty-web's translateToString concatenates getChars() per cell and returns '' for codepoint 0, never padding, so every blank cell vanishes from the string and takes its column with it (ghostty-web.js:4147). That is what produced 'Ididverifytheonethinginthatreply'. The desktop already knows: ghostty-terminal-pane.ts:759-763 builds row text cell by cell with codepoint \u003c 32 mapped to a space. After this bead nothing in the Companion calls translateToString, so the row-text problem stops existing here rather than being solved twice.\n\nAdd:\n- Touch scrolling over the grid, adapted into TerminalWheelController's event shape (pixel deltas, no modifiers) so one policy covers both gestures: normal screen moves the viewport, alternate screen sends page keys, a mouse-tracking program gets SGR reports. Synthesized bytes pass emitUser's gate unchanged, so a disarmed mirror still sends nothing.\n- The empty-history state: when the session is on the alternate screen there is no scrollback, and the phone says so in a line rather than showing nothing or showing repaint frames.\n\nDepends on hvir-coq: until sticky modes survive replay, the phone's emulator is on the normal screen for a full-screen session and the empty-history state would never be reached.","design":"Net-negative diff. The mount keeps its scale transform for the case where the desktop holds a wide grid, but the extent now sizes to the grid alone. Check whether the host still needs to be a scroller once nothing is drawn above the grid.","acceptance_criteria":"companion-mirror-history.ts and its test are gone; bufferLines is absent from the port, the adapter and the fixtures; typecheck and the full test suite pass. A mirror of a plain shell scrolls back through its scrollback by finger drag, in the session's colours and attributes. A mirror of a full-screen session shows the live screen and a stated reason there is no history. A disarmed mirror sends nothing on any gesture. The diff removes more lines than it adds.","notes":"hvir-w00 was filed against three observed defects. The spaces are retired by deletion here; the chrome defect is dissolved by hvir-coq; the theme defect was misdiagnosed and is its own bead.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-19T14:34:03Z","created_by":"sjarmak","updated_at":"2026-09-19T17:04:27Z","closed_at":"2026-09-19T17:04:27Z","close_reason":"Implemented and verified: check-adrs, check-seams, lint, typecheck and the full suite pass; the deletion is complete (no translateToString, bufferLines, MirrorHistory or .companion-terminal-history remain under the page tree except a test asserting their absence). Seven adversarial findings were confirmed and repaired by replacing the scroll seam with one reporting unconsumed distance; ten were refuted.","dependencies":[{"issue_id":"hvir-w00.3","depends_on_id":"hvir-coq","type":"blocks","created_at":"2026-09-19T10:34:25Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-w00.3","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T10:34:03Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-w00.3","depends_on_id":"hvir-w00.2","type":"blocks","created_at":"2026-09-19T10:34:25Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-w00.2","title":"ADR-053: the Companion mirror reads back through the emulator's viewport","description":"Write the record for the read-back decision taken on 2026-09-19, before the code changes.\n\nDecision: the phone's read-back is the emulator's own viewport. The page stops maintaining a parallel scrollback surface above the grid; scrolling moves the emulator's viewport over the scrollback it already holds, drawn by the same renderer that draws the live screen.\n\nThe reasoning to record:\n- Fidelity is the point. The mirror should be a continuation of the session's screen, not a transcript of it. A shell's scrollback is columnar (ls, git log --graph, diff, tables) and reflowing it to phone width destroys the thing being read back. Drawing it through the emulator makes colour, attributes, wide characters and theme exact by construction rather than by a second renderer that will drift.\n- The page's \u003cpre\u003e layer could not carry them at all. textContent has no colour, and the alternative (coalesced span runs, rebuilt incrementally, plus a full rebuild on every resize) is a DOM reimplementation of the emulator's drawing.\n- A full-screen session has no scrollback, and the phone should say so rather than show something. This only becomes true once hvir-coq lands; before it, the phone's emulator diverges onto the normal screen and stacks repaint frames that read as history.\n- Touch reuses the wheel policy. TerminalWheelController already decides the three cases; a finger drag adapts into its event shape rather than growing a second policy.\n- Leaving the viewport is a state the person can get stuck in, so a return-to-live control is part of the decision, not a later polish.\n\nRecords to reconcile: ADR-052:72-73 states as a consequence that 'the scrollback layer draws above the grid exactly as it does under ADR-050'. Amend that consequence; the rest of ADR-052 stands, and the Away size rule it establishes is unaffected. ADR-050 introduced the layer in implementation rather than in the record, so it needs no amendment beyond a pointer.\n\nState explicitly that the page reads where the viewport is (viewportY) and never what it says. That keeps the return-to-live control clear of ADR-019 and ADR-051's ban on screen inspection for attention: position is not content.","design":"One record, not two. The theme plumbing (hvir-w00 sibling) rides as a bead: inheriting the desktop's palette has no contested alternative worth a record.","acceptance_criteria":"docs/adr/ADR-053-*.md exists in the repo's ADR shape, carrying the decision, the rejected alternatives (page-owned span renderer; reflowed transcript; snap-to-live on output; a touch policy separate from the wheel policy), and the consequences. ADR-052's consequence at :72-73 is amended to point at it.","status":"closed","priority":1,"issue_type":"decision","assignee":"sjarmak","created_at":"2026-09-19T14:34:02Z","created_by":"sjarmak","updated_at":"2026-09-19T16:05:45Z","started_at":"2026-09-19T14:39:02Z","closed_at":"2026-09-19T16:05:45Z","close_reason":"Wave 1 implemented and verified: check-adrs, check-seams, lint, typecheck and the full suite (469 files, 4081 tests) pass. architecture:check is environment-blocked in this clone (scripts/architecture-github.mts requires origin jarmak-personal/hvir; this tree is sjarmak/hvir); architecture:report is clean with a budget-violation set identical to the pre-change tree.","dependencies":[{"issue_id":"hvir-w00.2","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T10:34:02Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-coq","title":"The PTY output tail drops sticky terminal modes, so every replay diverges from the session","description":"A reattaching emulator is fed `PtyOutputTail`, a flat 256 KB character window over the PTY stream (pty-output-tail.ts:2) that drops the oldest characters with no escape awareness. The sequences that set a terminal's sticky modes are emitted once, at program start, and then scroll out of that window. The worst case is the alternate screen: a full-screen harness writes \\x1b[?1049h once, so for any session that has since produced 256 KB of output the replay begins on the normal screen while the session is on the alternate. The replay is not sometimes wrong; for a long-running full-screen session it is always wrong.\n\nObserved through the Companion (hvir-w00). The phone's emulator, fed this tail, held Claude Code's full-screen repaints in its normal buffer, where they accumulated as scrollback: a thousand rows of successive redraws of one screen, complete with the feedback prompt, the 'Crunched for 37 s' status line, spinner frames and box borders, presented to the reader as if it were session history. The desktop showed a full-screen program with no scrollback at all, which is what a fullscreen harness actually has.\n\nBoth consumers are affected. pty-output-tail.ts:1 states the budget is 'shared by the renderer replay and the mirror tail', so the desktop's own reattach replay carries the same defect; it surfaces less because a live desktop pane rarely replays.\n\nApproach (fixed 2026-09-19): scan the retained stream for the sticky modes a reattached emulator cannot guess, keep the current set as state independent of the byte window, and emit it as a preamble ahead of the tail. Pure and unit-testable, lives beside the tail in main where the stream is, and fixes both consumers at once. 1049 is the one that matters most; settle the rest of the set while implementing (bracketed paste 2004, application cursor keys, mouse tracking modes, and anything else whose absence changes how the replayed bytes are interpreted).\n\nRejected: asking the desktop renderer's emulator for isAlternateScreen(). That puts main's replay correctness behind a renderer round-trip and fails exactly when the renderer is hidden or throttled, against ADR-001 and ADR-014.\n\nAlso noted while diagnosing, not necessarily this bead's to fix: the same raw slice can cut a CSI sequence in half, so the first bytes a replayed emulator sees may be a fragment.","design":"A sticky-mode scanner beside PtyOutputTail in src/main/pty. The tail keeps bytes; the scanner keeps mode state; the replay is preamble + tail. Do not widen the tail's budget to try to retain the enter sequence: the window is a memory bound, not a correctness mechanism, and anchoring it to the last mode change is unbounded.","acceptance_criteria":"A session that has produced more than the tail budget in output, on the alternate screen, replays onto the alternate screen: a fresh emulator fed the replay reports isAlternateScreen() true and holds no scrollback. A unit test drives the scanner over a stream whose mode-setting prefix has been dropped and asserts the preamble restores it. The desktop's reattach replay path uses the same preamble.","notes":"Found while grilling hvir-w00 on 2026-09-19. hvir-w00's implementation children depend on this: until it lands, no honest capture of a full-screen session's mirror is possible, because every capture records the divergence rather than the session.","status":"closed","priority":1,"issue_type":"bug","assignee":"sjarmak","created_at":"2026-09-19T14:33:27Z","created_by":"sjarmak","updated_at":"2026-09-19T16:05:46Z","started_at":"2026-09-19T14:39:01Z","closed_at":"2026-09-19T16:05:46Z","close_reason":"Wave 1 implemented and verified: check-adrs, check-seams, lint, typecheck and the full suite (469 files, 4081 tests) pass. architecture:check is environment-blocked in this clone (scripts/architecture-github.mts requires origin jarmak-personal/hvir; this tree is sjarmak/hvir); architecture:report is clean with a budget-violation set identical to the pre-change tree.","dependencies":[{"issue_id":"hvir-coq","depends_on_id":"hvir-w00.6","type":"blocks","created_at":"2026-09-19T11:04:32Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-w00.1","title":"Mock the phone history view in HTML and align on the design","description":"Design the phone's history view as standalone HTML before touching the Companion, and iterate on it with the maintainer until the shape is agreed.\n\nWork in three steps.\n\n1. Capture real data. Take the rows the page actually has to render, not invented text: dump CompanionTerminalPane.bufferLines() output for a real Claude Code turn and for a plain shell, as JSON (text plus the wrapped flag per row), into a fixture the mocks load. Do this from a test or a scratch harness against a recorded session; do not hand-write the sample. While capturing, settle the missing-space question with evidence: compare the captured row text against what the desktop terminal shows for the same turn, and say where the spaces go. Record the finding in this bead so it is not re-derived.\n\n2. Mock. Build standalone HTML pages under a scratch or docs path, opened in a phone-width viewport, that render the captured fixture. Produce at least two shape-distinct treatments, not two coats of paint on one idea, for example:\n   - terminal fidelity: keep the grid's columns, font and theme, carry colour and attributes across the seam, and let the column be a true continuation of the screen below it;\n   - reflowed reading: use the wrapped flag to rejoin lines the desktop broke, set the text at the phone's width in a proportional or narrower monospace size, and treat the history as a transcript rather than a screen.\n   Each mock shows the same captured turn so they can be compared directly. Include the chrome question in each: show what the view does with the feedback prompt, the status line, spinners and box borders.\n\n3. Align and hand off. Iterate with the maintainer on the mocks until one is chosen. Write down the chrome rule in words precise enough to implement. If the chosen design means the page decides what terminal content means, draft the ADR for it rather than smuggling the rule into a component. Then file the implementation children against this epic, sized as independently reviewable units, and close this bead with the chosen mock and the diagnosis recorded.\n\nOut of scope: any change under src/renderer/companion, src/main/companion, or the pane seam. This bead produces a decision and mocks, not shipped behaviour. The one exception is the read-only capture harness needed for step 1, which may be a test or a scratch script.\n","acceptance_criteria":"A captured fixture of real bufferLines output exists for a Claude Code turn and a plain shell; the cause of the missing spaces is diagnosed with evidence and recorded on the bead; at least two shape-distinct HTML mocks render that fixture at phone width; the maintainer chose one; the harness-chrome rule is written in implementable words; an ADR is drafted if the rule decides what terminal content means; implementation children are filed against the epic.","notes":"Superseded by the 2026-09-19 design session (see hvir-w00 notes). Both things this bead existed to produce were reached from source instead:\n\nThe missing-space diagnosis: ghostty-web's translateToString concatenates per-cell getChars() and returns '' for codepoint 0 without padding (ghostty-web.js:4147), so blank cells vanish and take their columns with them. The desktop already builds row text cell by cell for this reason (ghostty-terminal-pane.ts:759-763). No capture was needed; the bug is in the library's source and the workaround is in this repo's.\n\nThe design fork the mocks were to answer: settled as fidelity, and then settled further as fidelity by deletion. The read-back becomes the emulator's own viewport (hvir-w00.3, recorded in ADR-053), which no HTML mock can reproduce honestly, and which is a smaller change than mocking it because it is mostly removal. The phone check moved to a verification step on hvir-w00.3, done against a real session, with the option to stop there if it feels wrong in the hand.\n\nThe chrome rule this bead was to write in implementable words turned out not to be needed: the chrome was replay divergence (hvir-coq), not content the page has to interpret.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-19T13:40:49Z","created_by":"sjarmak","updated_at":"2026-09-19T14:34:59Z","closed_at":"2026-09-19T14:34:59Z","close_reason":"Superseded by hvir-w00.2 (ADR-053), hvir-w00.3 (read-back), hvir-w00.4 (return to live), hvir-w00.5 (theme) and hvir-coq (replay divergence). The diagnosis and the design decision it existed to produce were reached from source; the mocks would have prototyped a design nobody is building.","dependencies":[{"issue_id":"hvir-w00.1","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T09:40:48Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-w00","title":"The Companion mirror's history column is unreadable on the phone","description":"Why: with a mirror open the live grid at the bottom of the phone renders correctly, but the scrollback column above it does not. Three separate defects were observed on a real phone against a Claude Code session on 2026-09-19.\n\n1. Words lose the spaces between them. The column reads \"Ididverifytheonethinginthatreply\" where the terminal shows \"I did verify the one thing in that reply\". Whatever the cause, the text as drawn is close to unparseable.\n2. The column is flat white on black while the grid below it carries the terminal's theme, so the two halves do not read as one surface even though they are meant to.\n3. The text carries the harness's own TUI chrome, which means nothing to a person reading on a phone: a \"How is claude doing this session?\" feedback prompt, a \"Crunched for 37 s\" status line, spinners, and box-drawing borders. The phone shows everything the emulator holds, including the parts that only made sense as a full-screen layout on a desktop.\n\nThe first is a bug. The second and third are design questions that need a person's eye before any code changes, which is why the first child is an HTML mock rather than an implementation.\n\nStructural facts (mapped 2026-09-19, all on feat/beads-panel):\n- The column is one \u003cpre\u003e element. companion-terminal-mount.ts:79-83 creates it as .companion-terminal-history and appends it above the grid box in the same surface.\n- companion-mirror-history.ts:19-21 builds its content: lines.map(line =\u003e line.text).join('\\n'), written with textContent at most every 80 ms (HISTORY_REFRESH_MS), keeping a view that was at the newest line at the newest line.\n- The rows come from the CompanionTerminalPane seam. companion-terminal-pane.ts:20-25 defines CompanionBufferLine as { text, wrapped }: text only, no colour, no attributes. The `wrapped` flag is carried across the seam and never used by the history text, so the emulator already tells the page where the desktop broke a line and the page ignores it.\n- ghostty-companion-pane.ts:96-105 produces those rows with line.translateToString(false) over the active buffer.\n- companion-terminal-mount.ts:213-216 takes every row before the screen's own rows as the scrollback, bounded by HISTORY_LINE_LIMIT (1000).\n- styles.css:439-443 is the whole presentation: margin 0, color #e8eaee, white-space pre. The colour is hardcoded and is the one piece of the mirror that ignores the terminal theme.\n- fit() at companion-terminal-mount.ts:228-238 scales the history with the grid by the same fitWidthScale transform, so while the desktop holds the size the column is drawn at the desktop's width and then shrunk. While the phone holds the size under ADR-052 it is drawn at the phone's own grid unscaled.\n\nOpen questions for the design session, none of them settled:\n- What is actually removing the spaces? translateToString(false) is documented in this codebase as keeping the width, so either that is not what it does in ghostty-web, or the harness positions its text with cursor moves and never writes the intervening cells, or something downstream collapses them. Diagnose from captured data before designing around it.\n- Which sessions does this view even apply to? A Claude Code session with tui=fullscreen draws on the alternate screen and has no scrollback at all (see the alternate-screen memory and ADR-052), so history above the grid should be empty for it. The strings observed are Claude Code's, so either that session was not in fullscreen mode, or what is showing is the primary buffer from before or after the alternate screen. The answer changes what the view is for.\n- How much may the phone reinterpret what the terminal drew? Suppressing or demoting harness chrome means the page is reading content and deciding what it means. ADR-019 and ADR-051 forbid screen inspection for attention; display is not attention, and ADR-050 puts emulation on the phone by design, but the boundary should be stated in a record rather than assumed. If the answer is a rule rather than a tweak, this needs an ADR.\n- Does the column keep terminal fidelity (colour, attributes, monospace columns) or become reflowed prose sized for a phone? Those are different products. The mock is how we find out which one is wanted.\n","design":"Approach fixed by the maintainer on 2026-09-19: mock the view in standalone HTML first and iterate on it there until the UI and UX are agreed, then port the agreed design into the Companion. No changes under src/renderer/companion until a mock is chosen.\n\nImplementation children are deliberately not filed yet. They cannot be specified before the design exists, and filing them now would presume the answer. The mock child ends by filing them.\n","acceptance_criteria":"The phone's read-back is the emulator's own viewport, in the session's own theme, with a way back to live. A full-screen session shows no history and says why. The page keeps no parallel scrollback surface. ADR-053 records the decision and reconciles ADR-052. hvir-coq landed first.","notes":"IMPLEMENTED 2026-09-19 across three commits on feat/beads-panel: 07fd726, 3d572be, 5d1cbed. All gates green at each step (check-adrs, check-seams, lint, typecheck, 469 test files / 4130 tests). architecture:check is environment-blocked in this clone (scripts/architecture-github.mts requires origin jarmak-personal/hvir; this tree is sjarmak/hvir); architecture:report is clean and its budget-violation set is identical to the pre-change tree.\n\nHow the three observed defects resolved, against the epic's original acceptance:\n\n1. Words losing their spaces: retired by deletion, not fixed. ghostty-web's translateToString concatenates per-cell getChars() and returns '' for codepoint 0 without padding, so blank cells vanished and took their columns with them. Nothing in the Companion calls translateToString any more, because the page no longer renders row text at all.\n\n2. The column unthemed against the grid: diagnosed wrong in the original filing. Neither half was themed; the grid runs ghostty-web's default palette because the Companion has no theme plumbing at all. The clash was the deleted \u003cpre\u003e's hardcoded #e8eaee against that default. Deleting the \u003cpre\u003e removes the clash; the mirror still runs the default palette, which is what it did before. Inheriting the desktop's theme is hvir-w00.5, deferred with its blocker recorded.\n\n3. Harness chrome in the history: dissolved, not suppressed. It was never history. The retained PTY window dropped the alternate-screen enter, so the phone replayed onto the normal screen while the session was on the alternate one and stacked full-screen repaints as if they were scrollback. hvir-coq fixes the replay; a full-screen session now has no scrollback and the page states that. No chrome rule and no content-interpretation ADR were needed, because nothing in the shipped design reads what the screen says.\n\nThe acceptance clause 'the design was agreed from an HTML mock before any Companion code changed' was deliberately not met. hvir-w00.1 was closed as superseded: the fork the mocks existed to settle was answered from source, and the chosen answer is a live emulator viewport under a finger, which no static page can reproduce. The phone check moved to a verification step on hvir-w00.3.\n\nNOT YET VERIFIED ON A PHONE. Every gate here is a unit or integration test against a faked emulator. The scroll direction, the delta-to-rows feel under a real finger, the soft-keyboard interception, and the return-to-live control's placement have never run against the real ghostty build on a real device. The companion-mirror-away smoke scenario is the only altitude in this repo where the real emulator runs, and it was not extended. That is the remaining risk and it is the next thing to do.","status":"in_progress","priority":1,"issue_type":"epic","assignee":"sjarmak","created_at":"2026-09-19T13:40:25Z","created_by":"sjarmak","updated_at":"2026-09-19T17:59:02Z","started_at":"2026-09-19T14:39:02Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-0zt","title":"Desktop renderer: pty geometry event and a held-size terminal presentation","description":"Main tells the owning renderer when a mirror changed a PTY's size and when it reclaimed (new renderer event in src/shared/ipc/terminal.ts, published through the same path as pty:mirror-input). terminal-runtime.ts stops sending pty:resize from its own fit while the size is held and applies the mirror's cols/rows to the pane; TerminalPane gains a held-geometry presentation (fit suspended but rendering on) in which the ghostty pane draws the grid at the held cols/rows in the top-left of its container with the remainder blank and a one-line notice that the Companion holds the size. On reclaim the fit controller resumes and the next fit re-asserts the pane's own size through pty:resize. Keep it behind the TerminalPane seam; ghostty-terminal-fit.ts already has suspend/resume.","acceptance_criteria":"Runtime tests: a held geometry event resizes the pane and suppresses pty:resize; reclaim resumes fit and sends one pty:resize with the pane's own size; the seam test still assigns the pane; the notice renders only while held; nothing blocks the paint (no synchronous layout in the event handler).","status":"closed","priority":1,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-19T02:11:18Z","created_by":"sjarmak","updated_at":"2026-09-19T03:18:45Z","started_at":"2026-09-19T02:36:40Z","closed_at":"2026-09-19T03:18:45Z","close_reason":"Landed as 5e4a390 + 908a9b7 on feat/beads-panel: pty:mirror-geometry renderer event and main notice, runtime holds geometry and suppresses its own pty:resize while held, TerminalPane.setHeldGeometry with top-left held grid and themed notice, reclaim resumes fit. Two independent reviews, no blocking findings; theme tokens, duplicate-notice test, and mark extraction fixed. Full gates pass (4020 tests). Pane still 837 lines; follow-up bead filed for the FileLinkProvider split.","dependencies":[{"issue_id":"hvir-0zt","depends_on_id":"hvir-98g","type":"parent-child","created_at":"2026-09-18T22:11:39Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-0zt","depends_on_id":"hvir-ag2","type":"blocks","created_at":"2026-09-18T22:11:41Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-jro","title":"Companion page: the phone pane fits its area and requests the PTY's size while Away","description":"Add POST /api/sessions/:handle/resize (bounded body {cols, rows}, validated like input; refused with the lease's reason) in companion-api-routes.ts, routed to the mirror lease's resize. On the phone, companion-terminal-mount.ts measures the terminal area with the pane's cell metrics at a fixed readable font (a fit controller for the companion pane, engine-neutral through the CompanionTerminalPane seam) and asks for that geometry when the mirror opens and on area changes (orientation, keyboard via interactive-widget=resizes-content), debounced. When the geometry frame that arrives equals the requested size the surface renders at scale 1 with the phone's own scrollback; any other geometry keeps today's scaled-mirror column (history above a bottom-pinned grid). A refusal (desktop focused) is shown as a one-line status, not an error. Nothing resizes while the lease is not live.","acceptance_criteria":"Page tests: opening a mirror while the server reports away sends one resize with the measured cols/rows and renders unscaled once the matching geometry frame lands; a 'desktop-focused' refusal leaves the scaled view and shows the status line; a later geometry frame from the desktop (reclaim) returns the page to the scaled column; no resize is sent after 'ended'.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-19T02:11:18Z","created_by":"sjarmak","updated_at":"2026-09-19T03:20:16Z","closed_at":"2026-09-19T03:20:16Z","close_reason":"Landed as b244a78 + 73d0bd0 on feat/beads-panel: POST /api/sessions/:handle/resize with 409 desktop-focused refusal, away on the snapshot, phone fit controller at a fixed 15px font with serialized requests, unscaled rendering when the geometry matches the request. Two independent reviews; all findings fixed in the follow-up. Full gates run on the merged head follows.","dependencies":[{"issue_id":"hvir-jro","depends_on_id":"hvir-98g","type":"parent-child","created_at":"2026-09-18T22:11:39Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-jro","depends_on_id":"hvir-ag2","type":"blocks","created_at":"2026-09-18T22:11:42Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-9uw","title":"ADR-052: the Companion mirror may hold the PTY's size while the desktop is Away","description":"Write docs/adr/ADR-052 partially superseding ADR-050's 'a mirror never resizes the PTY' clause for exactly this case: a live mirror lease while ActionableAttentionSet.away() is true. Record the arbitration rule (phone owns size while Away; desktop reclaims on focus), the defaults (desktop draws the held grid top-left with a notice; phone fits a fixed readable font; most recent phone resize wins), and the rejected alternatives listed on the epic. Update ADR-050's lifecycle header (Superseded by ADR-052 | partial | ...) and the index in docs/design.md; update the contract comment at src/main/pty/pty-contract.ts:148. Follow ADR-051's header format; npm run check-adrs must pass.","acceptance_criteria":"check-adrs passes; ADR-050 header lists ADR-052 as a partial superseder with the exact clause; design.md index entry present; no progress checklists in the ADR.","status":"closed","priority":1,"issue_type":"task","assignee":"sjarmak","created_at":"2026-09-19T02:11:17Z","created_by":"sjarmak","updated_at":"2026-09-19T02:25:46Z","started_at":"2026-09-19T02:18:53Z","closed_at":"2026-09-19T02:25:46Z","close_reason":"Landed as 1619375 on feat/beads-panel: ADR-052 written, ADR-050 header and design.md index updated, pty-contract.ts comments updated. check-adrs, lint, typecheck, tests pass; architecture:check blocked by non-canonical origin in this clone.","dependencies":[{"issue_id":"hvir-9uw","depends_on_id":"hvir-98g","type":"parent-child","created_at":"2026-09-18T22:11:38Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-ag2","title":"PTY supervisor: mirror resize door with Away arbitration and geometry provenance","description":"Add resize(cols, rows) to the mirror lease (pty-contract.ts PtyMirrorLease, pty-mirror-lease.ts) and a supervisor door beside the renderer's. The supervisor records who last set each entry's geometry (renderer | mirror) and admits a mirror resize only while the lease is live, the instance is unchanged, and the desktop is Away (inject a Pick of ActionableAttentionSet exposing away()/observe from the composition root, the way AwayPush is wired). When away flips to false while a mirror holds the size, main publishes a reclaim so the owning renderer re-fits (child: renderer geometry event). Refusals fail closed with a reason like mirrorWriteRefusal (add 'desktop-focused'). Geometry changes from either source keep flowing to mirrors through publishGeometry. Dimensions clamped with terminalDimension() as the IPC handler does.","acceptance_criteria":"Supervisor tests: mirror resize applies and publishes geometry while away; refused with a reason while a window is focused, after release, after instance change; renderer resize while mirror holds size is still accepted and flips provenance back to renderer; away-\u003efocused transition emits the reclaim exactly once per hold; no mirror bytes in any error message.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-19T02:11:17Z","created_by":"sjarmak","updated_at":"2026-09-19T02:44:33Z","closed_at":"2026-09-19T02:44:33Z","close_reason":"Landed as 0fdb813 + 16b240a on feat/beads-panel: lease resize with desktop-focused refusal, supervisor Away door with geometry provenance, onMirrorGeometry held/reclaim events, terminalDimension moved to src/main/pty. Two independent reviews: no blocking findings; renderer-door gating rejected (contradicts acceptance and OS-driven resizes); reclaim-on-exited-entry nit fixed.","dependencies":[{"issue_id":"hvir-ag2","depends_on_id":"hvir-98g","type":"parent-child","created_at":"2026-09-18T22:11:38Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-ag2","depends_on_id":"hvir-9uw","type":"blocks","created_at":"2026-09-18T22:11:41Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"hvir-98g","title":"Companion mirror owns the PTY size while the desktop is Away (ADR-052)","description":"Why: a Claude Code session in fullscreen TUI mode (the user's ~/.claude/settings.json sets \"tui\": \"fullscreen\") draws on the alternate screen, so the emulator has no scrollback. On the phone the mirror can only show the desktop's grid scaled to the phone's width: a short strip pinned at the bottom with black above it, nothing to scroll, and the conversation history unreachable. Verified 2026-09-18 by spawning claude in a pty and seeing CSI ?1049h. Blink.sh avoids this by being the terminal: the PTY is sized to the phone. tmux faces the same two-client question and answers it with window-size (smallest / latest / manual).\n\nDecision (user approved 2026-09-18): the phone owns the PTY's size while the desktop is Away. A mirror opened while no hvir window is focused resizes the PTY to the phone's own grid, so a full-screen program lays out for the phone and fills it. When any desktop window gains focus the desktop reclaims: its fit controller resizes the PTY back and the phone returns to the scaled-mirror view it has today. Away is the existing ActionableAttentionSet.away() signal that already gates Push, so the rule the user learns is one they know: away means the phone is the terminal.\n\nRejected: phone owns the size whenever a mirror is open regardless of focus (tmux latest; squashes the desktop while you sit at it with the phone beside you); an explicit 'take the size' control on the phone (one more control on a page that was just stripped to one view, and a tap every time); scaling rows to the phone's height and panning sideways (the removed fill-height view); resizing nothing and only paging the program with PageUp/PageDown from a drag (leaves the black area).\n\nDefaults taken: while the phone holds the size the desktop draws the grid at the phone's dimensions in the top-left of its pane, remainder blank, with a one-line notice (as tmux marks a smaller client). The phone uses a fixed readable font and derives cols/rows from its terminal area, recomputed on orientation change and keyboard show. When two phones hold the same PTY the most recent resize wins. ADR-052 partially supersedes ADR-050's 'never resizes the PTY' clause (ADR-050 lines 37-38, 46-47; revisit clause 112-115) and pty-contract.ts:148.\n\nStructural facts (mapped 2026-09-18): main records geometry per entry (pty-supervisor.ts entry.geometry, updated only in resize(); publishGeometry reaches mirrors only). Desktop resize: TerminalFitController (ghostty-terminal-fit.ts) -\u003e terminal-runtime.ts pty:resize -\u003e main/ipc/features/terminal.ts -\u003e PtySupervisor.resize gated by requireOwned. No pty:geometry event back to the renderer exists; TerminalPane has no fixed-size mode (fit only stops under presentation 'hidden'). Mirror lease reads geometry once at attach and forwards onGeometry while live; lease has no resize. Focus: electron-window-manager.ts -\u003e ApplicationAttention.setOwnerFocused -\u003e ActionableAttentionSet.away().\n\nChildren are the seams: ADR, supervisor door + arbitration, renderer geometry event + held-size pane, phone fit + resize request, docs + device verification.","acceptance_criteria":"With the desktop unfocused, opening the mirror on a fullscreen Claude Code session fills the phone's terminal area edge to edge with no scaling; focusing a desktop window returns the desktop pane to its own size within one fit cycle and the phone shows the scaled mirror; a shell session's history column still works as today; no resize is accepted from a mirror while a desktop window is focused, after the lease ended, or for a changed instance; npm run verify passes; runbook and companion compass describe the rule.","notes":"All five children implemented on feat/beads-panel (1619375..8ebbdfe, 12 commits). Gates on the merged head: check-seams, check-adrs, lint, typecheck, npm test (468 files, 4056 tests) pass. npm run verify cannot complete in this clone: architecture:check requires the canonical repository (origin is sjarmak/hvir). Remaining: on-device verification (hvir-xyl), push, and a follow-up bead for the ghostty pane's line count.","status":"in_progress","priority":1,"issue_type":"epic","created_at":"2026-09-19T02:10:41Z","created_by":"sjarmak","updated_at":"2026-09-19T03:26:41Z","started_at":"2026-09-19T02:18:52Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-sz4","title":"Companion fit-width view: show the scrollback above the grid instead of black space below it","description":"Fit width scales the desktop's grid to the phone's width and leaves the rest of the area black. Draw the emulator's scrollback rows above the grid at the same scale and font, so the area shows one continuous column of history and live screen that the page scrolls, anchored at the live screen. The grid stays the desktop's and the PTY is never resized.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-19T01:10:14Z","created_by":"sjarmak","updated_at":"2026-09-19T01:15:42Z","closed_at":"2026-09-19T01:15:42Z","close_reason":"Scrollback drawn above the grid in fit-width","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-7h2","title":"Companion reflow view: fill the area's height and pin the newest line to the bottom","description":"With a short buffer the reflow page leaves black space under the text. Fill the host by flex rather than a percentage height, and anchor the text to the bottom edge so the newest output sits just above the controls, as in a terminal, with any spare room above.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-19T01:04:18Z","created_by":"sjarmak","updated_at":"2026-09-19T01:05:16Z","closed_at":"2026-09-19T01:05:16Z","close_reason":"Reflow page fills by flex, text pinned to the bottom edge","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-e22","title":"Companion mirror: reflowed reading view as the default, fit-width and fill-height as alternates","description":"On a phone the mirror opens in fill-height, which shows a window a few screens wide onto the desktop grid and reads as very zoomed in; fit-width shrinks a wide grid to unreadable text. Add a reflow view that renders the emulator's buffer as wrapped text at a readable size, filling the area vertically and scrolling natively, and make it the default. Keep fit-width and fill-height as the other two views of the same three-way toggle. The phone still never resizes the PTY.","status":"closed","priority":1,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T23:02:43Z","created_by":"sjarmak","updated_at":"2026-09-18T23:19:46Z","started_at":"2026-09-18T23:03:00Z","closed_at":"2026-09-18T23:19:46Z","close_reason":"Shipped on feat/beads-panel: reflow view (default), working badge, workspace groups","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-3k2.3","title":"Phone mirror layout: terminal fills the screen, touch scrolling through scrollback, compact controls, fill-height zoom","description":"The mirror view today gives the terminal a fit-to-width strip and spends the rest of the phone screen on the Arm typing button, the Esc/Tab/Ctrl-C/arrows/Enter key row, the text field with Send, and empty space below. Rework the layout in src/renderer/companion/src (terminal view, styles.css): the terminal area takes all viewport height below a one-line header; the controls collapse into one compact bar at the bottom (arm toggle plus a horizontally scrollable key strip and the text field shown only while armed); no empty region below the terminal. Touch scrolling over the terminal scrolls the emulator's scrollback (ghostty-web viewport), not the page, so the person can read earlier output; the page itself does not scroll while a mirror is open. Add a zoom toggle in the header between fit-to-width (current) and fill-height, where the terminal is scaled so its rows fill the available height and the person pans horizontally by touch; both are CSS transforms of the desktop geometry, and the phone still never sends a resize (ADR-050). Preserve arming semantics from hvir-gvt.3 (disarm on hide, lease end, inactivity).","acceptance_criteria":"With a mirror open the terminal occupies the viewport below a single header line and the controls bar is at most two compact rows; nothing empty remains below the terminal. Dragging on the terminal scrolls its scrollback and does not scroll the page. The zoom toggle switches between fit-to-width and fill-height with horizontal panning, and no resize is ever sent. Page tests cover the scroll routing, the zoom modes, and that the control bar hides the text field while disarmed.","status":"closed","priority":1,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T18:31:49Z","created_by":"sjarmak","updated_at":"2026-09-18T20:05:42Z","started_at":"2026-09-18T19:23:36Z","closed_at":"2026-09-18T20:05:42Z","close_reason":"Committed on epic/hvir-3k2-prompt-attention; verified by vitest, typecheck, lint, seams, an Electron visual run at phone size, two reviews and a delta re-review","labels":["attention","companion","terminal"],"dependencies":[{"issue_id":"hvir-3k2.3","depends_on_id":"hvir-3k2","type":"parent-child","created_at":"2026-09-18T14:31:48Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-3k2.3","depends_on_id":"hvir-3k2.2","type":"blocks","created_at":"2026-09-18T14:32:12Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-3k2.2","title":"Surfaces: prompt badge and message on the terminal rail, desktop Sessions view, Companion list and mirror header, plus the smoke probe","description":"Show the prompt kind and its message everywhere attention shows. Terminal rail (src/renderer/src/terminal/TerminalRail.tsx, TerminalRailCompact.tsx): badge text 'prompt' with the body as the accessible title. Desktop Sessions view (src/renderer/src/sessions/sessions-overview-model.ts and its components): the attention fact shows 'Prompt' and the message beside it; prompt rows sort with the other actionable rows. Companion list (src/renderer/companion/src/sessions-list.tsx, styles.css): companion-badge-prompt style and the message under the row title. Companion mirror view (the terminal view component in src/renderer/companion/src): one line above the terminal showing the row's current prompt message while present, gone when cleared. Push (src/main/companion/push-sink.ts): the prompt kind's line is the body, already bounded. Smoke: extend src/main/smoke/attention-away-probe.ts or add a sibling scenario that writes an OSC 9 notification into a terminal while Away and asserts a prompt entry with its body reaches main's actionable set and appears on the Companion row. Depends on child 1's contracts.","acceptance_criteria":"Each surface renders 'prompt' with the message for a prompt entry and renders ready and bell exactly as before. Page and component tests cover the rail badge, the Sessions fact, the Companion row, and the mirror header. The smoke run proves an OSC 9 notification produces a prompt entry with its body in main while Away and on the Companion row.","status":"closed","priority":1,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T18:31:48Z","created_by":"sjarmak","updated_at":"2026-09-18T19:23:35Z","started_at":"2026-09-18T18:57:03Z","closed_at":"2026-09-18T19:23:35Z","close_reason":"Committed on epic/hvir-3k2-prompt-attention; verified by vitest, typecheck, lint, seams, the companion-prompt-away smoke, two reviews","labels":["attention","companion","terminal"],"dependencies":[{"issue_id":"hvir-3k2.2","depends_on_id":"hvir-3k2","type":"parent-child","created_at":"2026-09-18T14:31:47Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-3k2.2","depends_on_id":"hvir-3k2.1","type":"blocks","created_at":"2026-09-18T14:32:12Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"hvir-3k2.1","title":"Attention model and contracts: prompt kind with its message end to end, cleared by mirror input","description":"Turn the pane's notification event (OSC 9 / OSC 777, translated in src/renderer/src/terminal/ghostty-terminal-events.ts) into a 'prompt' attention with a bounded body instead of a bell. Change terminal-pane-event-coordinator.ts so bell stays {bell:true} and notification (both sources) yields a notification effect with the body; add onNotification beside onBell in terminal-runtime-options.ts and thread it through TerminalView, TerminalSessionRuntimes, TerminalDeck to TerminalWorkspace where it raises prompt attention with the body. Renderer state: add 'prompt' to TerminalAttentionState (src/shared/terminal-attention.ts), priority above idle and bell in terminal-attention.ts, a promptBody field on TerminalSession (terminal-workspace-model.ts) cleared on session-focused, labels 'Prompt'/'prompt', actionable count and entries include prompt with body, fingerprint includes the body. Clearing: input that reaches the terminal from the Companion mirror (onMirrorInput in terminal-runtime.ts) clears a prompt attention and only a prompt attention; recordInput still arms idle. Shared contract: ActionableKind gains 'prompt', ActionableAttentionEntry gains optional body present only for prompt, isActionableAttentionEntry validates key set, kind, and body bound (define one shared constant, 120 chars, reused by push). Main: MainActionableEntry, fromRenderer, fingerprint carry the body (src/main/attention/actionable-attention-set.ts). Sessions projection: SessionsAttentionValue gains 'prompt' and the row carries the prompt text (src/shared/sessions-projection.ts, sessions-projection-coordinator.ts mapping). Companion rows: CompanionRow gains promptBody, ATTENTION_VALUES gains 'prompt', exact-key validation admits the optional field (src/shared/sessions-companion.ts, src/main/companion/companion-rows.ts). Push: describeTerminal passes the body as the line through the existing bound (push-describe.ts). No screen inspection anywhere. Governing record ADR-051.","acceptance_criteria":"A notification event raises prompt attention with its body immediately without an Enter or quiet period, and does not raise a bell. A prompt outranks idle and bell; a later notification replaces the body. Mirror input clears a prompt attention and leaves idle and bell untouched; desktop focus still clears everything. Bodies over the bound are truncated at the renderer and rejected by the wire validators. The body reaches main's set, the Sessions projection row, the Companion row, and the push line, each proven by a test. Existing terminal-attention, actionable-attention-contract, sessions-companion-contract, companion-rows, push-describe and event-translation tests are extended rather than weakened.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T18:31:47Z","created_by":"sjarmak","updated_at":"2026-09-18T18:57:02Z","closed_at":"2026-09-18T18:57:02Z","close_reason":"Committed on epic/hvir-3k2-prompt-attention; verified by focused and full vitest, typecheck, lint, seams, two reviews","labels":["attention","companion","terminal"],"dependencies":[{"issue_id":"hvir-3k2.1","depends_on_id":"hvir-3k2","type":"parent-child","created_at":"2026-09-18T14:31:46Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-3k2","title":"Terminal notification prompts as actionable attention with their message, on the desktop and the phone","description":"## Motivation\n\nA harness blocked on a permission prompt and a harness that finished its turn both show as Ready (quiet after Enter, ADR-019). The person cannot tell which sessions need a decision, on the desktop Sessions view or the Companion page. Once a row is Ready it stays Ready until desktop focus, so a second prompt in the same session is invisible, and a prompt answered from the phone mirror leaves the row flagged.\n\nClaude Code with preferredNotifChannel iterm2 writes OSC 9 into the PTY a few seconds after a permission prompt appears (body: Claude needs your permission) and when it finishes a turn (Claude is waiting for your input). Probed on 2026-09-18 with Claude Code 2.1.277 under TERM_PROGRAM=hvir: the sequence arrived about 6 s after the prompt, the terminal was silent meanwhile. hvir's pane already translates OSC 9 / OSC 777 into a notification event with title and body (ghostty-terminal-events.ts) and then collapses it into a bell in terminal-pane-event-coordinator.ts, dropping the body.\n\n## What this epic does\n\nGoverning record: ADR-051. A terminal notification raises a prompt attention immediately (no arming, no quiet wait) carrying its bounded body; the body crosses renderer actionable entries, main's actionable set, the Sessions projection row, the Companion row, and the Push line. A prompt entry outranks Ready and Bell; a later notification replaces the body. Clearing: desktop focus (ADR-009) and, for the prompt entry only, input from the Companion mirror. Output resuming never clears it. OSC 9 stops mapping to bell; BEL stays the bell. hvir does not set the harness's notification channel; the runbook documents it.\n\n## Children\n\n1. Attention model and contracts: prompt kind with body end to end (renderer state, shared contract, main set, sessions projection, companion rows, push describe), mirror-input clearing, tests.\n2. Surfaces: terminal rail badge, desktop Sessions view, Companion list row and mirror header, push line, smoke probe variant, tests.\n3. Docs: runbook section (setting, delay, what each surface shows), design.md index entry and notices, COMPASS notes.\n\nOrder: 1, then 2, then 3.\n\n## Acceptance\n\n- [ ] A Claude Code permission prompt in an hvir terminal shows within seconds as a prompt with its message on the terminal row, the desktop Sessions view, the Companion list, and the mirror view\n- [ ] A second prompt in the same session shows again without a desktop focus in between\n- [ ] Answering from the phone mirror clears the prompt entry; Ready and Bell are unaffected by phone input\n- [ ] A notification body never exceeds its bound at any boundary and is validated on the wire\n- [ ] Sessions with a harness that emits no notification keep the ADR-019 Ready behavior exactly\n- [ ] npm run verify passes; the attention-away smoke proves a prompt entry with its body reaches main while Away","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-09-18T18:30:48Z","created_by":"sjarmak","updated_at":"2026-09-18T20:21:12Z","closed_at":"2026-09-18T20:21:12Z","close_reason":"All four children integrated on epic/hvir-3k2-prompt-attention with a coherence fix; awaiting smoke re-run and push","labels":["attention","companion","terminal"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-gvt.4","title":"Settings typing permission and mirror-input attention arming, proven under throttling","description":"Add 'Allow typing from the Companion' to Settings \u003e Companion (src/renderer/src/settings/sections/CompanionSettings.tsx, companion-settings.ts, companion-config-store.ts), default off, persisted in companion.json and shown in the section status. When main writes mirror input, notify the owning renderer with the PTY id and bytes over a new terminal event; the renderer's terminal runtime records it as terminal input so ADR-019 arming (recordInput in use-terminal-attention-controller.ts) happens for a phone-submitted Enter. No phone action clears attention. Add a smoke scenario in src/main/smoke that submits a prompt from a mirror while Away with the window hidden and background-throttled and asserts the next Ready and one Push.","acceptance_criteria":"Toggling the permission persists and is reflected in the input route. A phone Enter arms Ready detection in the renderer exactly as a desktop Enter does. A mirror-submitted prompt yields Ready and one Push while Away under throttling in the smoke run. Viewing or typing from the phone never clears attention.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T13:50:59Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:21Z","closed_at":"2026-09-18T17:26:21Z","close_reason":"Implemented on epic/hvir-gvt-companion-terminal-mirror (02472df..c939b01); verified by vitest, typecheck, lint, seams, ADR guards and the companion-mirror-away smoke scenario","labels":["attention","companion","terminal"],"dependencies":[{"issue_id":"hvir-gvt.4","depends_on_id":"hvir-gvt","type":"parent-child","created_at":"2026-09-18T09:50:58Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.4","depends_on_id":"hvir-gvt.2","type":"blocks","created_at":"2026-09-18T09:51:01Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-gvt.3","title":"Phone page: ghostty-web terminal view with fit-to-width, on-screen keys, and input arming","description":"In src/renderer/companion: render a selected live terminal with ghostty-web through the engine-neutral pane seam (src/renderer/src/terminal/terminal-pane.ts, ghostty-terminal-pane.ts) at the desktop's cols/rows, scaled to fit the phone width; never send resize. Add .wasm to the Companion asset allowlist (companion-assets.ts and companion-http.ts content-type maps) and 'wasm-unsafe-eval' to the page CSP in src/renderer/companion/index.html. Input starts disarmed: an explicit arm control enables typing, and it disarms on page hide, lease end, and inactivity. Provide on-screen keys for Enter, Escape, Tab, arrows, and Ctrl-C beside free text entry; send exact bytes to the input route. Replace the raw 'Transcript unavailable (reason)' text with the plain sentences the desktop uses in sessions-transcript-coordinator.ts.","acceptance_criteria":"The wasm module loads from /assets on the phone page and a live terminal renders the tail then live output. The phone never sends a resize. Keystrokes are dropped client-side while disarmed and sent as exact bytes while armed; hiding the page disarms. Each on-screen key sends its documented byte sequence. Unavailable reasons render as plain sentences. Page tests cover arming, disarm on hide, and byte mapping.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T13:50:58Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:20Z","closed_at":"2026-09-18T17:26:20Z","close_reason":"Implemented on epic/hvir-gvt-companion-terminal-mirror (02472df..c939b01); verified by vitest, typecheck, lint, seams, ADR guards and the companion-mirror-away smoke scenario","labels":["companion","terminal"],"dependencies":[{"issue_id":"hvir-gvt.3","depends_on_id":"hvir-gvt","type":"parent-child","created_at":"2026-09-18T09:50:58Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.3","depends_on_id":"hvir-gvt.1","type":"blocks","created_at":"2026-09-18T09:51:01Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"hvir-gvt.2","title":"Companion service and routes: one mirror lease per page over SSE and POST input","description":"In CompanionSessionsService (src/main/companion/companion-sessions.ts), select on a row that is live, connected, and has a livePty opens a mirror lease on that instance under the page's companion demand owner; rows without a live hvir PTY keep the transcript path. One mirror per page; reselect replaces it. Add a terminal SSE event on /api/events carrying tail, bytes, and geometry, and POST /api/sessions/:handle/input that writes exact bytes under the lease with a bounded body. Input is refused unless the Settings typing permission is on. closePage, closeAll (revocation and shutdown), and PTY exit release the lease. Route table in companion-api-routes.ts; wire ports through companion-owner.ts and src/main/index.ts.","acceptance_criteria":"Selecting a live hvir terminal row streams its tail and live bytes to that page only. A second page selecting the same row gets its own mirror. Reselect, page close, revocation, and PTY exit each end the mirror and emit the matching event. Input route returns 403 when typing is disallowed in Settings and 409 when the lease ended or the instance changed; accepted bytes reach the PTY unchanged. Existing companion-sessions and companion-server tests keep passing.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-09-18T13:50:57Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:20Z","closed_at":"2026-09-18T17:26:20Z","close_reason":"Implemented on epic/hvir-gvt-companion-terminal-mirror (02472df..c939b01); verified by vitest, typecheck, lint, seams, ADR guards and the companion-mirror-away smoke scenario","labels":["companion","terminal"],"dependencies":[{"issue_id":"hvir-gvt.2","depends_on_id":"hvir-gvt","type":"parent-child","created_at":"2026-09-18T09:50:57Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.2","depends_on_id":"hvir-gvt.1","type":"blocks","created_at":"2026-09-18T09:51:00Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"hvir-gvt.1","title":"PTY supervisor: retained output tail and a mirror lease door on an exact instance","description":"Add tail retention to PtyStreamAttachment (src/main/pty/pty-stream-attachment.ts) that is independent of the renderer replay, which is consumed on attach; keep the same 256K char budget. Add PtySupervisor.attachMirror(instanceId, handlers) beside the owner-gated attach: bound to the immutable instanceId, not the renderer owner, so it survives transferRendererSession. The returned lease exposes write(data) that goes through the supervisor bound to the instance and fails closed when the instance changed, the PTY exited, or the lease ended. Handlers receive the tail on attach, live data, the current cols/rows on attach and on every resize, and exit. A mirror never resizes, kills, spawns, or transfers. Bytes are never logged or written to diagnostics.","acceptance_criteria":"A mirror attached after the renderer receives the retained tail then live bytes. Mirror survives a renderer document transfer and ends on PTY exit. write through an ended lease, a mismatched instance, or an exited PTY throws and writes nothing. Resize from the renderer reaches the mirror as geometry; the mirror cannot resize. Tests at the supervisor altitude cover each fail-closed case.","status":"closed","priority":1,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T13:50:56Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:20Z","started_at":"2026-09-18T14:04:58Z","closed_at":"2026-09-18T17:26:20Z","close_reason":"Implemented on epic/hvir-gvt-companion-terminal-mirror (02472df..c939b01); verified by vitest, typecheck, lint, seams, ADR guards and the companion-mirror-away smoke scenario","labels":["companion","terminal"],"dependencies":[{"issue_id":"hvir-gvt.1","depends_on_id":"hvir-gvt","type":"parent-child","created_at":"2026-09-18T09:50:56Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"hvir-7hu","title":"ADR-050: The Companion mirrors a live hvir terminal and carries its user's keystrokes","description":"Drafted at docs/adr/ADR-050-companion-live-terminal-mirror.md, indexed in docs/design.md, with a partial superseded-by notice on ADR-049. Uncommitted until the epic is approved. Decides: a mirror lease on an exact PTY instance as a second reader never a second owner; a retained output tail in the stream attachment; bytes cross uninterpreted and the phone emulates with ghostty-web at the desktop's geometry; phone keystrokes are exact user bytes bound to the instance, opt-in via Settings and per-mirror arming; attention classification stays in the renderer with no new clearing rule; eligibility equals the desktop's Interact.","status":"open","priority":1,"issue_type":"decision","created_at":"2026-09-18T13:50:55Z","created_by":"sjarmak","updated_at":"2026-09-18T13:50:55Z","labels":["adr","companion","terminal"],"dependencies":[{"issue_id":"hvir-7hu","depends_on_id":"hvir-gvt","type":"relates-to","created_at":"2026-09-18T09:51:03Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-gvt","title":"Live terminal mirror and keyboard for hvir-owned sessions in the Companion","description":"## Motivation\n\nThe Companion (ADR-049) lists every session on the phone, and for a gas city row it\nreads the transcript, answers the pending interaction, and sends a message. For a\nterminal hvir launched itself it shows the row and refuses the transcript with\n`not-projected`, because there is no supervisor API behind it.\n\nThat is nearly every session we run. Claude Code and Codex are started from a shell\ninside an hvir terminal, and gas city workers are joined with `gc session attach`\ninside an hvir terminal. On the desktop, Interact on such a row borrows the live\nterminal pane with the whole keyboard. On the phone the same row is a dead end, and\nthe moment that matters most, a permission prompt or option picker waiting in\nClaude Code's terminal UI, exists only on that screen. No transcript file carries it.\n\n## What this epic does\n\nA Companion page can select a live hvir-owned terminal row and see that terminal,\nrendered on the phone by ghostty-web at the desktop's geometry, from a retained tail\nof output onward. With typing allowed in Settings and armed on the page, keystrokes\nreturn to that exact PTY instance. A prompt submitted from the phone still produces\nthe next Ready and the next Push, because the renderer keeps classifying. Nothing\nchanges on the desktop: one owner per PTY, no new clearing rule, no resize from the\nphone.\n\nGoverning record: ADR-050, which partially supersedes ADR-049's exclusion of a\nterminal screen and terminal input from the Companion.\n\n## The seams, and why this is not an escalation\n\nMain already sees every PTY byte. `PtyStreamAttachment` holds a set of data\nlisteners and fans output out to all of them; only the public door,\n`PtySupervisor.attach`, is gated on the renderer owner and generation through\n`requireOwned`. The replay buffer (`MAX_INITIAL_REPLAY_LENGTH`, 256 K chars) is\nconsumed when the renderer attaches, so a second reader gets nothing today; a\nretained tail is the one new piece of state. Writes already go through\n`PtySupervisor.write` under the same gate, so a mirror write is a second caller of\nan existing authority bound to an instance rather than an owner.\n\nThe Sessions projection already carries the identity the mirror needs: a live row's\n`livePty.handle` is the PTY `instanceId`, and its row handle is the PTY id. The\nCompanion sessions service already holds one demand owner per page\n(`kind: 'companion'`), opens and closes pages on the SSE stream, and closes every\npage on revocation. Mirror leases hang off that lifecycle.\n\nThe phone page is a second Vite entry served from the Companion's flat asset\nbundle. ghostty-web loads its WebAssembly through a Vite `?url` import, so the\nmodule is already emitted into that bundle. Two things block it on the phone today:\nthe asset allowlist in `companion-assets.ts` and `companion-http.ts` has no `.wasm`\ncontent type, and the page CSP in `src/renderer/companion/index.html` lacks\n`'wasm-unsafe-eval'`.\n\nADR-017, ADR-026, and ADR-032 forbid hvir composing text into a PTY. Phone\nkeystrokes are the user's exact bytes, uncomposed and unappended, bound to an exact\ninstance that fails closed on change. Those records are untouched.\n\n## Decisions taken before decomposition\n\n- Mirror, not transfer. The renderer stays the PTY's owner; the mirror is bound to\n  the instance and survives a renderer document swap, ending only on PTY exit, page\n  close, revocation, shutdown, or reselect.\n- Bytes cross uninterpreted. Main keeps a tail and forwards; the phone emulates.\n  No headless emulator in main.\n- Geometry follows the desktop. The phone renders the desktop's columns and rows,\n  scaled to fit, and never resizes the PTY.\n- Typing is opt-in three times: a Settings permission defaulting to off, per-mirror\n  arming on the page, and disarming on hide, lease end, or inactivity.\n- Attention stays in the renderer. Main notifies the owning renderer of mirror\n  input so ADR-019 arming happens where it always has. No phone action clears\n  attention.\n- Eligibility equals the desktop's Interact: live, connected, live PTY. External\n  rows without an hvir terminal keep the transcript path.\n- Transport reuses what exists: mirror output rides the page's SSE stream as a new\n  event type; input rides bounded POST bodies. A socket is a later optimization,\n  not a decision.\n\n## Child issues\n\n1. PTY supervisor: retained output tail and a mirror lease door on an exact\n   instance. Tail retention in `PtyStreamAttachment` independent of renderer\n   replay; `attachMirror(instanceId, handlers)` returning a lease with `write`,\n   geometry on attach and on change, ending on exit; tests at the supervisor\n   altitude including the fail-closed cases.\n2. Companion service and routes: one mirror lease per page, `select` on an\n   eligible live row opens it, a `terminal` SSE event carries tail, bytes, and\n   geometry, `POST /api/sessions/:handle/input` writes under the lease, and\n   page close, reselect, and revocation release it.\n3. Phone page: terminal view in ghostty-web through the engine-neutral pane seam,\n   `.wasm` in the asset allowlist and CSP, fit-to-width scaling, on-screen keys\n   (Enter, Escape, Tab, arrows, Ctrl-C), arm and disarm controls, and the\n   `not-projected` dead end replaced by the same plain wording the desktop uses.\n4. Settings and attention: the typing permission in Settings \u003e Companion with its\n   status, the mirror-input notice from main to the owning renderer recorded as\n   terminal input, and a smoke scenario proving a phone-submitted prompt yields\n   Ready and Push while Away under background throttling.\n5. Docs: operator runbook section for the mirror and typing permission, ADR index\n   entries (already drafted with ADR-050), and the Companion COMPASS.\n\nOrder: 1 first; 2 and 3 in parallel on 1; 4 on 2; 5 last.\n\n## Acceptance\n\n- [ ] Selecting a live hvir terminal row on the phone shows that terminal's screen,\n      including a Claude Code permission prompt that is on the desktop's screen\n- [ ] The mirror survives a desktop reload of the renderer document and ends when\n      the PTY exits, the page closes, or pairing is revoked\n- [ ] With typing disallowed in Settings, no input route accepts bytes; with it\n      allowed but unarmed on the page, no keystroke reaches the PTY\n- [ ] An armed Enter from the phone answers a Claude Code prompt on the desktop,\n      and the row leaves the actionable set when output resumes\n- [ ] A prompt submitted from the phone produces the next Ready and one Push while\n      Away, with the desktop window hidden and throttled\n- [ ] The phone never resizes the PTY; the desktop's geometry is what the phone\n      renders\n- [ ] Mirror bytes never appear in logs, diagnostics, or push text\n- [ ] Rows without a live hvir PTY keep their existing transcript or unavailable\n      path, with a plain sentence instead of a reason code\n","status":"closed","priority":1,"issue_type":"epic","assignee":"sjarmak","created_at":"2026-09-18T13:50:54Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:22Z","started_at":"2026-09-18T14:04:57Z","closed_at":"2026-09-18T17:26:22Z","close_reason":"All five children integrated on epic/hvir-gvt-companion-terminal-mirror; awaiting push and merge","labels":["companion","terminal"],"dependencies":[{"issue_id":"hvir-gvt","depends_on_id":"hvir-7hu","type":"relates-to","created_at":"2026-09-18T09:51:03Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-w00.4","title":"Return to live from a scrolled-back mirror","description":"Once the emulator's viewport is the read-back (hvir-w00.3), a person who scrolls up stays there: ghostty keeps its viewport while output arrives, which is the right behaviour (losing your place mid-read is worse than missing a line) but leaves no way to notice the session moved and no quick way back. A phone viewport is a few dozen rows against a thousand rows of scrollback, and there is no scrollbar to grab.\n\nShow one control, only while the viewport is off the bottom, that says the session has moved and returns to live in one tap.\n\nThe page reads where the viewport is (viewportY) and never what it says. That is position, not content, so it stays clear of ADR-019 and ADR-051's ban on screen inspection for attention; ADR-053 states the boundary.","design":"One control, one piece of state, appearing on a condition the emulator already exposes. No unread marker and no record of what arrived while away: that is a second piece of state plus a rule for when it clears, and nobody has asked for it.","acceptance_criteria":"Scrolling back from live shows the control; returning to the bottom hides it. Tapping it puts the viewport at live. Output arriving while scrolled back does not move the viewport. Nothing about the control reads the screen's text.","status":"closed","priority":2,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-19T14:34:22Z","created_by":"sjarmak","updated_at":"2026-09-19T17:58:32Z","started_at":"2026-09-19T17:10:28Z","closed_at":"2026-09-19T17:58:32Z","close_reason":"Implemented and verified: all five gates pass (check-adrs, check-seams, lint, typecheck, 469 files / 4130 tests). Every new test was mutation-probed against the source it pins. Three adversarial findings confirmed and repaired, six refuted; the repair also deleted viewportOffset() and releaseViewport as dead by construction.","dependencies":[{"issue_id":"hvir-w00.4","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T10:34:21Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-w00.4","depends_on_id":"hvir-w00.3","type":"blocks","created_at":"2026-09-19T10:34:26Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-w00.5","title":"The mirror inherits the desktop's terminal theme","description":"The phone's emulator has never seen the desktop's palette. createGhosttyCompanionPane passes no theme, so it runs ghostty-web's built-in default; grep for 'theme' across src/renderer/companion, src/main/companion and src/shared/sessions-companion.ts returns nothing. The desktop applies the user's TerminalColorTheme (ghostty-terminal-pane.ts:57, :327).\n\nThis is the real shape of hvir-w00's second observed defect. The epic read it as a themed grid beside an unthemed history column; in fact neither was themed, and the clash was the history \u003cpre\u003e's hardcoded #e8eaee against a different unthemed palette. Deleting the \u003cpre\u003e (hvir-w00.3) removes the clash and leaves the mirror on the wrong palette.\n\nCarry the desktop's theme to the mirror and apply it. The mirror should look like the session it mirrors: a person glancing at their phone should recognise their own terminal.\n\nPlumbing, to settle while implementing:\n- TerminalColorTheme and normalizeTerminalColorTheme live in src/renderer/src/terminal/ (terminal-pane.ts:20, terminal-palette.ts:70), which the page may not import. They are pure data and a pure function; move them to src/shared, where the page already imports from.\n- The companion wire carries the theme, validated at the boundary like every other field in sessions-companion.ts.\n- Where main learns the selected theme is an open question: the catalog and selection live in the desktop renderer today. Answer it before designing the wire field.\n\nRejected: a deliberate phone-tuned palette independent of the desktop's. It permanently makes the mirror look like a different terminal, which is the opposite of what a mirror is for.","design":"No phone-side override and no settings surface for it: that is a precedence rule nobody has asked for. Inherit, full stop.","acceptance_criteria":"A mirror opened against a session on a non-default theme draws in that theme, live screen and scrollback alike. The palette type and normalizer are in src/shared with no renderer dependency. The wire field is validated at the page boundary. Changing the desktop's theme while a mirror is open is covered one way or the other: either it follows, or the bead records why it does not.","notes":"BLOCKED and deferred out of hvir-w00's critical path on 2026-09-19, with evidence from the grounding pass.\n\nMain cannot see the selected palette at all. The theme ids live in the desktop renderer's localStorage under 'hvir:settings:v1' (src/renderer/src/settings/settings.ts:32) and the appearance under 'hvir:theme' (src/renderer/src/theme.ts:7), resolved to a palette per TerminalView at src/renderer/src/terminal/TerminalView.tsx:111-112. src/shared/ipc/companion.ts:9-20 carries only config read/save, pairing issue/revoke and status-changed; src/main/companion/companion-config-store.ts:24-30 persists only enabled, port, mirrorInputAllowed, credential and push. The only main-side read of that storage anywhere in the repo is localStorage.getItem('hvir:settings:v1') inside executeJavaScript in the smoke harness (src/main/smoke/renderer-lifecycle.ts:18,22,106), which is test scaffolding, not a production path.\n\nSo this bead is not 'apply a palette'. It is: a renderer to main publish channel (registered in src/main/ipc/features/terminal.ts, the only place ipc.handleSend exists, at :466-484, already 547 lines), a per-PTY palette store in main with a forget-on-exit hook, the publisher moved to src/renderer/src/terminal/terminal-runtime.ts (terminal-runtime-live-settings.ts cannot do it: its functions take only (pane, options) and never touch window.hvir), the palette type and normalizer moved to src/shared, and a new required field on the companion wire.\n\nIt also trips the architecture gate twice. scripts/architecture-hotspots.json:215-233 pins sha256 provenance for both scripts/generate-terminal-theme-catalog.mts and src/renderer/src/terminal/terminal-color-schema.mts; moving or editing either makes scripts/architecture-inventory.mts:288-300 throw 'Generated input identity mismatch' and fails npm run architecture:check. Rewriting the inputs path to fix it is classified as a relaxation by scripts/architecture-policy.mts:362-368, which ADR-040:75-91 says a consuming issue's review cannot authorize. Separately, .mts under src/shared is in neither tsconfig (tsconfig.web.json:9-15 lists src/shared/**/*.ts and src/renderer/**/*.mts, never src/shared/**/*.mts), so eslint's projectService errors on it.\n\nNothing regresses by deferring: the mirror stays on ghostty-web's default palette, which is what it does today. Before this is picked up it needs the architecture relaxation authorized on its own terms, and a decision on whether a live desktop theme change propagates to an open mirror (a 'theme' CompanionTerminalEvent variant modelled on 'geometry' would be safe, since companion-wire.ts:65-69 ignores unknown event names).","status":"open","priority":2,"issue_type":"feature","created_at":"2026-09-19T14:34:22Z","created_by":"sjarmak","updated_at":"2026-09-19T15:04:15Z","dependencies":[{"issue_id":"hvir-w00.5","depends_on_id":"hvir-w00","type":"parent-child","created_at":"2026-09-19T10:34:22Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-xyl","title":"Runbook, companion compass, and on-device verification of the Away size rule","description":"Update docs/runbooks/companion-operator.md (what the phone shows; troubleshooting rows for 'the mirror is a thin strip' and 'desktop pane shows a small grid with a notice') and src/main/companion/COMPASS.md (restamp sources_hash with node ~/.claude/skills/project-compass/compass-hash.mjs). Verify on a real phone against a fullscreen Claude Code session and a plain shell: fill, reclaim on desktop focus, scrollback on the shell. Record the observed behaviour in the closing reason, not in the ADR.","acceptance_criteria":"Runbook and compass describe the rule and the desktop notice; compass hash fresh; device check done for both session kinds.","notes":"Docs half landed as 8ebbdfe (runbook, companion COMPASS restamped). On-device verification pending the maintainer: fullscreen Claude Code fills the phone while Away; desktop reclaims on focus; shell history column; refusal status line; one resize per keyboard/rotation change. Close with observed behaviour.","status":"in_progress","priority":2,"issue_type":"task","assignee":"sjarmak","created_at":"2026-09-19T02:11:19Z","created_by":"sjarmak","updated_at":"2026-09-19T03:26:40Z","started_at":"2026-09-19T03:20:16Z","dependencies":[{"issue_id":"hvir-xyl","depends_on_id":"hvir-0zt","type":"blocks","created_at":"2026-09-18T22:11:43Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-xyl","depends_on_id":"hvir-98g","type":"parent-child","created_at":"2026-09-18T22:11:40Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-xyl","depends_on_id":"hvir-jro","type":"blocks","created_at":"2026-09-18T22:11:43Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-7hf","title":"Companion mirror: one fit-to-width view with scrollback above a bottom-pinned grid","description":"The mirror offers three views (reflow, fit width, fill height). Only the fit-width layout is wanted: scrollback drawn above the desktop's grid, scaled to the phone's width, the host scrolling over the two. The reflow page also leaks under the grid in the fit-width view because its display:flex rule overrides the hidden attribute, so the black area below the grid shows reflow text. Remove the other two views and the view control, pin the grid to the host's bottom edge when the column is shorter than the host, and update runbook and compass.","status":"closed","priority":2,"issue_type":"task","assignee":"sjarmak","created_at":"2026-09-19T01:28:09Z","created_by":"sjarmak","updated_at":"2026-09-19T01:41:07Z","started_at":"2026-09-19T01:28:14Z","closed_at":"2026-09-19T01:41:07Z","close_reason":"Single fit-to-width view with scrollback above a bottom-pinned grid; reflow and fill-height removed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-kr0","title":"Companion list: show a working badge for terminals the desktop sees working","description":"The Companion list badges only ready, bell, prompt and the turn state of external agents; an hvir terminal that is producing output shows nothing. Carry the renderer's working handles to main beside the actionable entries and show a working badge on the row.","status":"closed","priority":2,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T23:02:44Z","created_by":"sjarmak","updated_at":"2026-09-18T23:19:46Z","started_at":"2026-09-18T23:02:59Z","closed_at":"2026-09-18T23:19:46Z","close_reason":"Shipped on feat/beads-panel: reflow view (default), working badge, workspace groups","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-txz","title":"Companion list: group rows by workspace","description":"The Companion list is flat, with project / workspace repeated on every row. Group rows under one heading per workspace (project / workspace, with the host for ssh), groups ordered by name, rows inside keeping the desktop's actionable-first order.","status":"closed","priority":2,"issue_type":"feature","assignee":"sjarmak","created_at":"2026-09-18T23:02:44Z","created_by":"sjarmak","updated_at":"2026-09-18T23:19:46Z","started_at":"2026-09-18T23:02:59Z","closed_at":"2026-09-18T23:19:46Z","close_reason":"Shipped on feat/beads-panel: reflow view (default), working badge, workspace groups","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-3k2.4","title":"Docs: runbook section for prompt attention, design.md index and notices for ADR-051, COMPASS notes","description":"Extend docs/runbooks/companion-operator.md: how to make Claude Code emit the notification (preferredNotifChannel iterm2 in ~/.claude/settings.json, or --settings on the command line), the few-second delay Claude Code applies before notifying on a permission prompt, what each surface shows (terminal rail, Sessions view, Companion list, mirror header), that answering from the phone clears the prompt entry while Ready and Bell are unchanged, and the new mirror layout controls (zoom toggle, scrollback scrolling, compact bar). Confirm the ADR-051 entry and the ADR-009 and ADR-050 notices in docs/design.md match the relationship-line shape and that npm run check-adrs passes. Add the prompt kind and its bound to src/main/companion/COMPASS.md, and note the OSC 9 to prompt (not bell) translation in the terminal area's compass if one exists. Prose: declarative, concrete, no em dashes.","acceptance_criteria":"Runbook explains the harness setting, the delay, each surface, and the clearing rule. check-adrs passes. COMPASS names the prompt kind, its bound, and the mirror-input clearing owner.","status":"closed","priority":2,"issue_type":"task","assignee":"sjarmak","created_at":"2026-09-18T18:31:50Z","created_by":"sjarmak","updated_at":"2026-09-18T20:11:25Z","started_at":"2026-09-18T20:05:43Z","closed_at":"2026-09-18T20:11:25Z","close_reason":"Committed on epic/hvir-3k2-prompt-attention; check-adrs passes, compass hash restamped","labels":["attention","companion","docs","terminal"],"dependencies":[{"issue_id":"hvir-3k2.4","depends_on_id":"hvir-3k2","type":"parent-child","created_at":"2026-09-18T14:31:49Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-3k2.4","depends_on_id":"hvir-3k2.2","type":"blocks","created_at":"2026-09-18T14:32:13Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-3k2.4","depends_on_id":"hvir-3k2.3","type":"blocks","created_at":"2026-09-18T14:32:13Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-gvt.5","title":"Docs: runbook, ADR index, and Companion COMPASS for the terminal mirror","description":"Extend docs/runbooks/companion-operator.md with the mirror and the typing permission, including the geometry and first-frame notes from ADR-050's consequences. Confirm the ADR-050 entry and the ADR-049 notice in docs/design.md. Update src/main/companion COMPASS (or create it) with the mirror lease lifecycle and the wasm allowlist gotcha.","acceptance_criteria":"Runbook explains enabling typing, arming on the page, and what the phone shows for rows without a live PTY. design.md index and ADR-049 notice match the README's relationship-line shape. COMPASS names the mirror lease owner and the asset allowlist.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T13:51:00Z","created_by":"sjarmak","updated_at":"2026-09-18T17:26:21Z","closed_at":"2026-09-18T17:26:21Z","close_reason":"Implemented on epic/hvir-gvt-companion-terminal-mirror (02472df..c939b01); verified by vitest, typecheck, lint, seams, ADR guards and the companion-mirror-away smoke scenario","labels":["companion","docs","terminal"],"dependencies":[{"issue_id":"hvir-gvt.5","depends_on_id":"hvir-gvt","type":"parent-child","created_at":"2026-09-18T09:50:59Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.5","depends_on_id":"hvir-gvt.2","type":"blocks","created_at":"2026-09-18T09:51:02Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.5","depends_on_id":"hvir-gvt.3","type":"blocks","created_at":"2026-09-18T09:51:02Z","created_by":"sjarmak","metadata":"{}"},{"issue_id":"hvir-gvt.5","depends_on_id":"hvir-gvt.4","type":"blocks","created_at":"2026-09-18T09:51:03Z","created_by":"sjarmak","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"hvir-c29","title":"Split FileLinkProvider out of ghostty-terminal-pane.ts to bring the pane under the 800-line ceiling","description":"ghostty-terminal-pane.ts is 837 lines after the ADR-052 held-size presentation (809 before it). The held-size mark was already extracted to terminal-held-geometry-mark.ts; the remaining clean split is FileLinkProvider (an exported class of ~100 lines at the bottom of the pane with its own imports and a link-detection concern) into its own module behind the TerminalPane seam. Behaviour unchanged; the seam and lifecycle tests must stay green.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-19T03:16:44Z","created_by":"sjarmak","updated_at":"2026-09-19T03:16:44Z","dependency_count":0,"dependent_count":0,"comment_count":0}
